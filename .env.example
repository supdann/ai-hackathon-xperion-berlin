# =============================================================================
# Authentication
# =============================================================================
# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# =============================================================================
# OpenAI API (Required)
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
# Used for:
# - Chat models (gpt-4o, gpt-4o-mini, o1)
# - Text embeddings (text-embedding-3-small) for RAG semantic search
OPENAI_API_KEY=****

# =============================================================================
# Database - PostgreSQL with pgvector
# =============================================================================
# Railway PostgreSQL connection string with pgvector extension
# Example: postgresql://user:password@host:port/database
# Make sure pgvector extension is enabled: CREATE EXTENSION IF NOT EXISTS vector;
POSTGRES_URL=****

# =============================================================================
# Vercel Blob Storage (Optional - for file attachments)
# =============================================================================
# Instructions to create a Vercel Blob Store: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# =============================================================================
# Redis (Optional - for resumable streams)
# =============================================================================
# Instructions to create a Redis store: https://vercel.com/docs/redis
# Not required for basic functionality
REDIS_URL=****

# =============================================================================
# Required Environment Variables
# =============================================================================
# Minimum required for the app to work:
# - AUTH_SECRET (for authentication)
# - OPENAI_API_KEY (for chat models and embeddings)
# - POSTGRES_URL (for database with pgvector extension)
#
# Optional:
# - BLOB_READ_WRITE_TOKEN (for file attachments)
# - REDIS_URL (for resumable streams)
#
# Database Requirements:
# - PostgreSQL with pgvector extension enabled
# - Run: CREATE EXTENSION IF NOT EXISTS vector;
# - unified_promo_product table with embeddings (see RAG_INTEGRATION.md)
